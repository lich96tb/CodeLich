version: '2'
services:
    calm-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb calm-mongodb-config
    calm-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    calm-mongodb-config:
        image: mongo:3.6.3
        container_name: calm-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
