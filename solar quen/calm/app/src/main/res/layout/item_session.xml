<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="java.lang.Integer" />

        <variable
            name="session"
            type="com.melodymediation.sleepstories.data.room.Session" />
        <variable
            name="hasLock"
            type="Integer" />
    </data>

    <android.support.v7.widget.CardView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_marginRight="8dp"
            android:layout_marginBottom="9dp"
            app:cardElevation="15dp"
            app:cardMaxElevation="20dp"
            app:cardCornerRadius="20dp">

            <RelativeLayout
                android:layout_width="fill_parent"
                android:layout_height="213dp">

                <ImageView
                    android:id="@+id/iv_thumb_session"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    app:srcCompat="@drawable/birthday" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentBottom="true"
                    android:layout_centerHorizontal="true"
                    android:layout_marginBottom="55.7dp"
                    android:gravity="center"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tv_session_name"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:gravity="center_horizontal"
                        android:letterSpacing="0.01"
                        android:text="@{session.name}"
                        android:textAllCaps="true"
                        android:textColor="#ffffff"
                        android:textSize="20sp" />

                    <TextView
                        android:id="@+id/tv_session_time"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:gravity="center_horizontal"
                        android:letterSpacing="0.01"
                        android:text="@{session.duration}"
                        android:textColor="#ffffff"
                        android:textSize="13sp" />

                </LinearLayout>

                <RelativeLayout
                    android:id="@+id/rl_lock_session"
                    android:layout_width="30dp"
                    android:layout_height="30dp"
                    android:layout_marginBottom="8dp"
                    android:layout_marginRight="8dp"
                    android:background="@drawable/bg_lock"
                    android:layout_alignParentRight="true"
                    android:layout_alignParentBottom="true"
                    android:visibility="invisible"
                    android:gravity="center">

                    <ImageView
                        android:id="@+id/iv_lock_session"
                        android:layout_width="15dp"
                        android:layout_height="20dp"
                        app:srcCompat="@drawable/ic_15_white" />

                </RelativeLayout>

            </RelativeLayout>

        </android.support.v7.widget.CardView>
</layout>
